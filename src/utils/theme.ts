import { createTheme } from "@mui/material";

// Todo: Change button background color to black

export const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#fff",
    },
  },
  typography: {
    fontFamily: "Poppins",
  },
});
